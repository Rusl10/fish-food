// comment
// add code above, if we use fullscreen sections

/*let vh = window.innerHeight * 0.01;

document.documentElement.style.setProperty('--vh', `${vh}px`);
if (!isMobile.any()) {
    window.addEventListener('resize', function() {
        let vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', `${vh}px`);
    });
} else {
    // we don`t use resize on mobiles, beacause window.innerHeight is recalculated, when we hide/show adressbar during scrolling`
    // reset vh if orientation is changed
    function handleOrientationChange(event) {
        let vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', `${vh}px`);
    }
    var mql = matchMedia("(orientation:portrait)");
    mql.onchange = handleOrientationChange;

}*/